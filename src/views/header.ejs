<!DOCTYPE html>
<html>
  <head>
    <% if (locals.title) { %>
      <title><%= title %></title>
    <% } else  { %>
      <title>Bank Portal</title>
    <% } %>
    <link rel='stylesheet' type='text/css' href='/css/styles.css' />
  </head>
  <body>